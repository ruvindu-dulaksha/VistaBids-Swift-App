import SwiftUI

// MARK: - Custom Color for Translation Features
extension Color {
    // Translation-related colors
    static let translationBackground = Color.accentBlues.opacity(0.1)
    static let translationActiveText = Color.accentBlues
    static let translationCompletedBackground = Color.green.opacity(0.1)
    static let translationCompletedText = Color.green
    static let translationErrorBackground = Color.red.opacity(0.1)
    static let translationErrorText = Color.red
}
